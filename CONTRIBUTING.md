# Внесение вклада в Digital Escrow Network

Спасибо за интерес к нашему проекту! Мы приветствуем ваши вклады. Пожалуйста, прочитайте этот документ перед тем как начать.

## Кодекс поведения

### Наше обязательство

Мы, как члены, участники и лидеры сообщества, обязуемся создавать среду, свободную от преследований для всех, независимо от возраста, размера тела, видимой или невидимой инвалидности, этнической принадлежности, половых характеристик, гендерной идентичности и выражения, уровня опыта, образования, социально-экономического статуса, национальности, внешности, расы, религии или сексуальной идентичности и ориентации.

Мы обязуемся действовать и взаимодействовать так, чтобы способствовать открытой, гостеприимной, разнообразной, инклюзивной и здоровой сообществе.

### Стандарты

Примеры поведения, которые способствуют созданию позитивной среды, включают:

- Использование приветливого и инклюзивного языка
- Уважение к различным точкам зрения и опыту
- Конструктивное принятие критики
- Сосредоточение на том, что лучше всего для сообщества
- Проявление эмпатии к другим членам сообщества

### Применение

Примеры неприемлемого поведения включают в себя:

- Использование сексуализированного языка или образов
- Преследование, оскорбительные комментарии
- Личные или политические нападки
- Публичное или частное домогательство
- Публикация приватной информации без явного согласия
- Другое поведение, которое может обоснованно считаться неприемлемым в профессиональной среде

## Начало работы

### Требования для разработки

- Node.js 18+ (рекомендуется 20+)
- npm или yarn
- Git

### Подготовка окружения

1. **Форк репозитория**
   ```bash
   # Создайте fork на GitHub
   ```

2. **Клонирование вашего fork'а**
   ```bash
   git clone https://github.com/your-username/DigitalEscrowNetwork.git
   cd DigitalEscrowNetwork
   ```

3. **Добавление upstream**
   ```bash
   git remote add upstream https://github.com/fox-and-cat/DigitalEscrowNetwork.git
   ```

4. **Установка зависимостей**
   ```bash
   npm install
   ```

5. **Создание файла `.env`**
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашими значениями
   ```

## Процесс разработки

### Создание ветки

Используйте описательные имена веток:

```bash
# Для новой функции
git checkout -b feature/add-user-authentication

# Для исправления ошибки
git checkout -b fix/handle-null-pointer-exception

# Для документации
git checkout -b docs/update-api-reference
```

### Стандарты кода

#### TypeScript

- Используйте строгую типизацию (`strict: true`)
- Избегайте использования `any`
- Добавляйте type annotations где это необходимо

#### Именование

- Переменные и функции: `camelCase`
- Классы и интерфейсы: `PascalCase`
- Константы: `UPPER_SNAKE_CASE`

#### Структура файлов

```typescript
// 1. Импорты (отсортированы по группам)
import { dep1, dep2 } from 'external-lib';
import type { Interface } from 'types';

// 2. Типы и интерфейсы
interface MyInterface {}

// 3. Константы
const CONSTANT = 'value';

// 4. Основной код
export function myFunction() {}
```

### Linting

Перед commit'ом убедитесь что код проходит linting:

```bash
npm run lint
```

Для автоматического исправления:

```bash
npm run lint -- --fix
```

### Тестирование

Если вы добавляете новую функциональность, пожалуйста добавьте соответствующие тесты (тестовая структура будет добавлена в будущем).

### Написание commit'ов

Используйте конвенцию Conventional Commits:

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Типы commits

- **feat**: Новая функция
- **fix**: Исправление ошибки
- **docs**: Изменения документации
- **style**: Форматирование, отсутствующие точки с запятой и т.д.
- **refactor**: Рефакторинг кода
- **perf**: Улучшения производительности
- **test**: Добавление или обновление тестов
- **chore**: Изменения build процесса, зависимостей и т.д.

#### Примеры

```
feat(bot): add conversation support for deal creation

- Implement multi-step dialog for creating new deals
- Add validation for deal parameters
- Add keyboard inline for deal confirmation

Closes #42
```

```
fix(utils): prevent null pointer exception in validator

Previously, the validator would throw when receiving undefined input.
Now it properly handles null and undefined values.

Closes #15
```

## Отправка Pull Request

### Перед отправкой

1. **Обновите свою ветку**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

2. **Убедитесь что код работает**
   ```bash
   npm run build
   npm run lint
   npm run dev  # Протестируйте локально
   ```

3. **Сделайте clean commit'ы**
   ```bash
   git push origin feature/your-feature
   ```

### Описание PR

Пожалуйста, предоставьте:

- Четкое описание того что вы изменили и почему
- Ссылку на связанные issues (используйте `Closes #123`)
- Скриншоты если это UI изменения
- Инструкции для тестирования если необходимо

### Пример PR описания

```markdown
## Описание

Добавлена поддержка MongoDB persistence для хранения данных сделок между перезагрузками бота.

## Тип изменения

- [x] Новая функция
- [ ] Исправление ошибки
- [ ] Breaking change

## Связанные issues

Closes #45

## Чек-лист

- [x] Я следовал стайл гайду проекта
- [x] Я обновил документацию где необходимо
- [x] Мой код проходит `npm run lint`
- [x] Я протестировал изменения локально

## Как тестировать

1. Установите зависимости: `npm install`
2. Запустите MongoDB: `docker run -d -p 27017:27017 mongo`
3. Запустите dev сервер: `npm run dev`
4. Создайте новую сделку используя `/createdeal`
5. Проверьте что данные сохраняются в MongoDB
```

## Review процесс

- Минимум один одобрение необходимо перед merge
- Все conflicts должны быть разрешены
- CI checks должны проходить

## Вопросы?

Не стесняйтесь открывать [GitHub Issue](https://github.com/fox-and-cat/DigitalEscrowNetwork/issues) если у вас есть вопросы.

Спасибо за ваш вклад! 🎉
